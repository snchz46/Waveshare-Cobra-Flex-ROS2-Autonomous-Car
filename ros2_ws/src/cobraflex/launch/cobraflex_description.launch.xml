<launch>

    <!-- =========================================== -->
    <!-- Arguments                                    -->
    <!-- =========================================== -->
    <arg name="use_rviz" default="true"/>
    <arg name="urdf_file" default="$(find-pkg-share cobraflex)/urdf/my_robot.urdf.xacro"/>

    <arg name="use_sim_time" default="false"/>
    <arg name="slam_params_file" default="$(find-pkg-share cobraflex)/config/mapper_params_online_async.yaml"/>
    
    <!-- =========================================== -->
    <!-- robot_state_publisher                        -->
    <!-- =========================================== -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher" name="robot_state_publisher" output="screen">
        <param name="robot_description" value="$(command 'xacro $(var urdf_file)')"/>
        <param name="use_sim_time" value="$(var use_sim_time)"/>
    </node>

    <!-- =========================================== -->
    <!-- joint_state_publisher                        -->
    <!-- =========================================== -->
    <node pkg="joint_state_publisher" exec="joint_state_publisher" name="joint_state_publisher" output="screen">
        <param name="use_sim_time" value="$(var use_sim_time)"/>
    </node>

    <!-- =========================================== -->
    <!-- RViz (optional)                              -->
    <!-- =========================================== -->
    <group if="$(var use_rviz)">
        <node pkg="rviz2" exec="rviz2" name="rviz2" output="screen"
              args="-d $(find-pkg-share cobraflex)/rviz/mav1_description.rviz"/>
    </group>

    <!-- =========================================== -->
    <!-- SLAM Toolbox                                 -->
    <!-- =========================================== -->
    <node pkg="slam_toolbox" exec="async_slam_toolbox_node" name="slam_toolbox" output="screen"> <!-- online_async_launch -->
        <param name="use_sim_time" value="$(var use_sim_time)"/>
        <param name="slam_params_file" value="$(var slam_params_file)"/>
    </node>

    <node pkg="cobraflex" exec="cobraflex_odom.py" name="cobraflex_odom" output="screen">
        <param name="use_sim_time" value="$(var use_sim_time)"/>
    </node>

</launch>
